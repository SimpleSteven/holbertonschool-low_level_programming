#include <stdio.h>

/**
  * main - the function
  * @argc: the int
  * @argv: the char
  * Return: 0 if is succesful
  */

int main(int argc, char *argv[])
{
	(void)argv;
	printf("%i\n", argc - 1);

	return (0);
}
