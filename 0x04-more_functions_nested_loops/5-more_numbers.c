#include <unistd.h>
#include "holberton.h"

/**
 * more_numbers - writes the character c to stdout
 */

void more_numbers(void)
{
	int a, b;

	for (a = '0'; a <= ''; ++a)
	_putchar(a);
	_putchar('\n');
}
