#include <unistd.h>
#include "holberton.h"

/**
 * mul - writes the character c to stdout
 * @a: first int
 * @b: second int
 * Return: return 1 if is uppercase and 0 if isnt
 */

int mul(int a, int b)
{
	a = a * b;
	return (a);
}
