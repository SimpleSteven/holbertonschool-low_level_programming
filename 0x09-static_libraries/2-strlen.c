#include "holberton.h"

/**
 * _strlen - check the code for Holberton School students
 * @s: intb
 * Return: the length of the parameter
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
